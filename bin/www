#!/usr/bin/env node

/**
 * Module dependencies.
 */

var app = require('../app');
var debug = require('debug')('tma-api:server');
var http = require('http');

/**
 * Get port from environment and store in Express.
 */

//var port1 = normalizePort(process.env.PORT || '3334');
//app.set('port', port1);

//var port2 = normalizePort(process.env.PORT || '3335');
//app.set('port', port2);

var port3 = normalizePort(process.env.PORT || '9000');
app.set('port', port3);

/**
 * Create HTTP server.
 */

//var server1 = http.createServer(app).listen(port1).on('error', onError);
//server1.on('listening', onListening(server1));

//var server2 = http.createServer(app).listen(port2).on('error', onError);
//server1.on('listening', onListening(server2));

var server3 = http.createServer(app).listen(port3).on('error', onError);
//server1.on('listening', onListening(server3));

/**
 * Listen on provided port, on all network interfaces.
 */

//server.listen(port);
//server.on('error', onError);
//server.on('listening', onListening);

/**
 * Normalize a port into a number, string, or false.
 */

function normalizePort(val) {
  var port = parseInt(val, 10);

  if (isNaN(port)) {
    // named pipe
    return val;
  }

  if (port >= 0) {
    // port number
    return port;
  }

  return false;
}

/**
 * Event listener for HTTP server "error" event.
 */

function onError(error) {
  if (error.syscall !== 'listen') {
    throw error;
  }

  var bind = typeof port1 === 'string'
    ? 'Pipe ' + port1
    : 'Port ' + port1;

  // handle specific listen errors with friendly messages
  switch (error.code) {
    case 'EACCES':
      console.error(bind + ' requires elevated privileges');
      process.exit(1);
      break;
    case 'EADDRINUSE':
      console.error(bind + ' is already in use');
      process.exit(1);
      break;
    default:
      throw error;
  }
}

/**
 * Event listener for HTTP server "listening" event.
 */

function onListening(server) {
  var addr = server.address();
  var bind = typeof addr === 'string'
    ? 'pipe ' + addr
    : 'port ' + addr.port;
  debug('Listening on ' + bind);
}
